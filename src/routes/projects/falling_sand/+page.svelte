<script>
	// import falling_sand from '../../../falling_sand.wasm?init';

	// const importObject = {
	// 	imports: { imported_func: (arg) => console.log(arg) }
	// };

	// WebAssembly.instantiateStreaming(fetch(falling_sand), importObject).then((obj) =>
	// 	obj.instance.exports.exported_func()
	// );
</script>

<p>Falling Sand</p>
